<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hero List</title>
  </head>
  <body>
    <h3>
      <%= heroes.title %>
      </h3>
      <table style="border: 1px solid #000;">
        <tr>
          <th>id</th>
          <th>name</th>
          <th>role</th>
          <th>edit</th>
          <th>remove</th>
        </tr>
        <% for(let i= 0; i <heroes.data.length; i++) { %>
          <tr>
            <td><%= i + 1%></td>
              <td><%= heroes.data[i].name%></td>
                <td><%= heroes.data[i].role%></td>
                  <td>
                    <a href="/hero/<%= heroes.data[i].id %>">edit</a>
                  </td>
                  <td> <form action="/hero/remove" method="post">
                      <input type="text" value="<%= heroes.data[i].id %>" hidden
                      name="id"
                      />
                      <input type="submit" value="remove">
                    </form></td>
                </tr>
                <% } %>
                </table>
                <br>
                <br>
                <br>
                <h3>TAMBAH HERO</h3>
                <form action="/hero/add" method="post">
                  <input type="text" name="name" placeholder="isi nama hero" />
                  <input type="text" name="role" placeholder="isi nama role" />
                  <br>
                  <br>
                  <input type="submit" value="tambahkan">
                </form>
              </body>
            </html>